K.G.N. FASHION ZONE - LOGO REPLACEMENT V13

The uploaded logo artwork is preserved. These files are intended to replace exactly three existing files at the ROOT of the GitHub repository:

logo.png
icon-192.png
icon-512.png

Do not upload the ZIP itself. Extract it and upload these three PNG files in the repository root with EXACT names. Do not place them in a subfolder.

The existing website already references these filenames in index.html, admin.html, inventory.html and manifest.webmanifest. Do not edit cloud-config.js, Supabase or product images.

The existing GitHub connection was read-only for updates, so these generated assets have NOT been deployed to the live GitHub site.

If GitHub mobile UI refuses to overwrite identical names, keep the downloaded replacements safe, remove only the corresponding old image files in GitHub (not other files), commit, then upload the three new files and commit. During the interval the images may appear broken.

After successful Pages deployment, open the website in an Incognito tab to check the new logo. For an already installed home-screen PWA icon, remove the old shortcut/app and reinstall from Chrome if the OS keeps the old icon.

// Fill ONLY your Supabase project URL and PUBLISHABLE key (sb_publishable_...).
// Both values are safe to put in browser code when RLS is correctly configured.
// NEVER put a secret key, service_role key, database password, or admin login here.
window.KGN_CLOUD_CONFIG = Object.freeze({
  url: '',
  publishableKey: ''
});
